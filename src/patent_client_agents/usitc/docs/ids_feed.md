## IDS JSON feed

Public investigations metadata is published at https://ids.usitc.gov/investigations.json.
Use filters client-side to avoid loading unnecessary rows; the MCP tool streams the
entire JSON array.
